"""
Fallback module for AI-based PDF extraction when standard extraction fails.
"""


def pdf_fallback(input_path, output_path):
    """
    Placeholder fallback: will be replaced with AI-based extraction methods.
    For now, just signals that fallback was triggered.
    """
    print("\tfallback")
